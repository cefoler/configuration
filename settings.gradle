ext {
    projectName = "configuration"
    prefix = ":$projectName"
}

rootProject.name = projectName

include "$prefix-core"
include "$prefix"

project("$prefix-core").projectDir = file("$rootDir/core")
project("$prefix").projectDir = file("$rootDir/configuration")
